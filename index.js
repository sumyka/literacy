document.addEventListener('DOMContentLoaded', function () {
    const button = document.getElementById('about');

    button.addEventListener('click', function () {
        event.preventDefault();
        alert('まだそのページは作ってません！！！')
    })
});
